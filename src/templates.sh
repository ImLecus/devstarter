#!/bin/bash

# Template names
# Make sure to put a color before the name
# (check ./colors.sh)
TEMPLATE_NAMES=(
    "${BLUE}C"
    "${RED}C with CMake"
    "${c_QUARZUM}Quarzum"
    "${c_CPP}C++"
    "${BLUE}Python"
    "${c_WEB}Web (Vanilla)"
    "${YELLOW}Node JS"
)

# Template identifiers
# Make sure the sh file has the same name
# as the identifier.
TEMPLATE_IDS=(
    "c"
    "cmake"
    "quarzum"
    "cpp"
    "python"
    "web"
    "node"
)